import { Component } from '../ecs/Component.js';

/**
 * A tag component indicating that an entity is currently being hovered by the user's cursor.
 * It has no data or methods. Its presence is its state.
 */
export class Hovered extends Component {}
